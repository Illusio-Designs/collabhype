# Production defaults — used by Vercel builds.
NEXT_PUBLIC_API_BASE_URL=https://api.collabhype.in
NEXT_PUBLIC_DEMO_MODE=0
# Razorpay publishable key. Safe to commit: NEXT_PUBLIC_* is compiled into the
# client bundle and shipped to every visitor anyway. The matching key SECRET is
# server-side only and must never appear in this file.
NEXT_PUBLIC_RAZORPAY_KEY_ID=rzp_live_TEUVjKHGFeNi5c
